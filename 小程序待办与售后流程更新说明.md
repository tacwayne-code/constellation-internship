# 小程序待办与售后流程更新

## 本次修复

- 小程序消息页不再固定空数组，改为真实的“售后待办”。工程师只见本人未完成工单，派单员见售后未完成工单。读取现存工单，因此通知上线前的工单也可见；不依赖企微 outbox，不把它称作未读消息。
- 加载中、网络失败、无售后权限、真实空列表分别展示，支持下拉刷新和分页。点击进入售后模块（不是直接跳详情），每次刷新短时 SSO 票据；不缓存 ASS 登录 token。
- 首页入口防连点、刷新问候语；个人页显示中文角色；身份请求 15 秒超时。
- 工程师不能读取他人工单详情、接单或提交维修记录；全局工单/工程师名录/统计接口仅派单员。完成记录需已接单且仍属于自己，条件更新防重复提交。

## Hermes 服务端部署

1. 备份 ASS 数据库（SQLite backup API）、配置与现有镜像。保持 WeCom 密钥、通知开关、绑定、平台连接等配置原样。
2. 拉取本说明所在提交，仅重建 service，其他服务不用更新。本次无新表和历史数据迁移。
3. 验证 /health；/inbox 未登录401；用已有工程师合法会话读 /inbox，仅返回他的未完成工单。不得伪造微信票据或借用他人会话，无法合法取得会话时交由本人真机验收。
4. 不创建工单、不接单、不完成线上测试记录，不补发企微消息。已有工单和通知记录数量应保持不变。
5. 确认后台就绪后通知用户上传企业服务小程序新体验版。服务端先更新，旧体验版仍可用；新体验版依赖 /inbox。

## 客户端上传与真机验收（必须）

- 使用仓库中的“企业服务小程序”目录，不是“售后小程序/miniprogram”独立旧工程。AppID 沿用当前正式 AppID，保留用户本地 project.private.config.json，不覆盖本地个人设置。
- 确认 request 合法域名含 https://service.inspiri.cn 和身份网关的 https://crm.inspiri.cn，测试不要关闭合法域名校验。
- 微信开发者工具编译并上传新体验版后，重新进入。工程师消息页应看到本人现有未完成工单；派单员可以看到全局未完成工单。
- 点击待办进入售后，在我的任务查看相同单号；当前不自动定位详情，不是可点击企微消息卡片。
- 对明确的测试工单，用户可手动接单、填写记录并完成，返回消息页/下拉刷新后该单应消失，历史页仍可查看。线上这一步必须由用户主动执行。
- 断网应提示加载失败而不是“暂无”；无售后角色显示未开通售后及 CRM 消息未接入。CRM 消息中心尚未实现。

## 验证边界

本地自动化使用隔离数据库、模拟微信接口和模拟 Page/wx 运行环境；覆盖接口权限、接单完成、重复提交、消息加载与跳转逻辑，不发送真实通知、不改线上数据。
原生 WXML/WXSS 不能由普通 Playwright 浏览器直接渲染。本机未完成微信开发者工具/手机的新版视觉验收，不能把逻辑测试当作完整真机验证。没有声明所有 CRM/ASS 问题已修完。

测试命令：售后 backend 运行 `python -m unittest test_wecom_notifications -v` 及 `python test_security.py`；仓库根运行 `node --test 企业服务小程序/tests/messages.test.cjs`。

回滚：恢复旧 service 镜像以及原小程序体验包即可；不回退数据库覆盖新增业务记录。新体验包遇旧后端会显示加载失败，先回退客户端或恢复新后端。
