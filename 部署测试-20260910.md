# 2026-09-10 CRM / ASS / 企业服务管理后台部署测试

发布分支：codex/crm-ass-release-20260910。三套应用源码已集中在此分支，无需从不同工作区拼接。基于 50bc6e3，保留统一客户库连接和既有报备功能。

## 更新内容

- CRM 大字体、橙色界面，客户列表简化；新增客户电话必填。
- 联系人仅填写人本人可见，销售经理也不能看其他人填写的联系人；原库联系人保留但不展示。
- 客户和拜访地址支持定位解析、按企业名搜索地图地址。定位需 HTTPS，地图查询需已有 AMAP_WEB_SERVICE_KEY；工商查询未接入。
- 小程序移除报销审批与员工管理权限；人员使用企业服务管理后台既有“人员与角色”。后台新增报销审批。
- ASS 和 CRM 增加本人提交工单列表、分页、流转时间线和维修说明；其他人的工单不可凭编号查看。旧单缺失的流转时间不补造。

## 部署

1. 备份 CRM JSON/员工库、ASS 数据库、identity、平台数据库和各服务配置。保留现有账号、企微绑定、历史工单和维修记录。不导入本地演示数据。
2. 使用本分支更新 CRM、ASS、统一业务平台，重建 CRM 和平台前端，并同时部署对应后端。保留现有统一客户库连接配置。
3. ASS 启动会新增 work_order_events 表，不删旧表或记录。既有 SSO_SHARED_SECRET 保持一致且不轮换；既有 JWT、企微和 Odoo 配置不变。
4. 报销内部连接：CRM 与平台设置同一专用随机 CRM_EXPENSE_ADMIN_SECRET；平台设置 CRM_EXPENSE_API_URL 为平台容器可达的 CRM 内部服务基础地址（无 /api 后缀）。密钥仅放服务器环境文件，不放前端。未配置时后台提示未连接，小程序审批仍禁止。
5. 不新增公网端口；手机定位用正式 HTTPS 入口。此次不更改原生小程序 project.config.json / private 配置。

## 验收

- CRM 新客户空电话拒绝保存；两位销售及经理分别查看客户，不能看到他人的联系人或原库联系人。
- 小程序不出现员工管理/报销审批，旧接口对销售经理同样拒绝。后台管理员审批后，申请人能看到结果；驳回必填原因，重复审批拒绝。
- 工程师和 CRM 销售各自提交需求→在本人提交记录中出现→派单给另一工程师→接单→维修完结，各阶段状态与维修说明可追踪；另一提交人不能读取该单。
- 返回报备页或刷新后能查询；票据超过 10 分钟需从原入口重新进入。跨 ASS/CRM 账号不按姓名合并。
- 验证既有客户同步、派单通知、账号授权正常，测试单按用户要求保留。

测试在隔离数据中执行，尚未生产部署，不发送真实通知。


## 补充：单次售后联系人

- 新建报备和派单工单必须填写本次报修联系人及电话；不自动取 CRM、Odoo 或上一单联系人。服务地址仍可取企业地址。
- 联系资料写入新增 order_contacts 表，只关联本工单，不写入公司通讯录或客户主档。后续现场/报修对接人变化追加记录，原记录保留；已结束工单不可追加。
- 提交人从“我提交的工单”详情查看和补充；派单员及当前承派工程师从工单详情操作；其他工程师拒绝访问。原报修电话保留，更换人员使用联系记录入口。
- ASS 后端、web/request.html、request.js、app.js、index.html、新增 order-contacts.js 必须一起发布。启动自动增加 order_contacts 表。不得清库。
- 如使用独立 ASS 原生派单页面，需同步发布 miniprogram/pages/paidan/create 下改动（新建接口增加必填联系人）；企业服务小程序的 H5 入口只需更新服务器网页。
- 验收：同公司新建两单不带出上一单联系方式；补充现场联系人后原报修联系人仍存在；未承派工程师不可查看；Odoo/CRM 主档保持原样。

## 2026-09-11 派单按钮反馈修复

ASS web/app.js 修复创建工单必填项无明确提示、上传/API 异常无反馈的问题；点击后显示提交中并禁止连点；已保存但刷新失败时提示查列表，不重复创建。更新 ASS 网页后重新进入 H5 即可，数据库无变更。390px 手机模拟接口验证了缺联系人/电话、服务端拒绝及修正后提交成功。


## 派单员企微新申请提醒

只更新 ASS。CRM/工程师报备写入成功后，同一事务增加 SERVICE_REQUEST 通知；重复申请不重复入队。已有工单不补发；派单前收到提醒，已经安排工程师的待发提醒取消。原工程师派单通知保持不变。

部署前备份 ASS 数据库、现有镜像及 wecom.env。核验杨艳桢在 ASS users 的真实 id=3、role=paidan，企微 userid=Yang（沿用已核实交接，不按姓名猜身份）。在原 wecom.env 绑定 JSON 中追加 "3":"Yang"，保留所有原工程师绑定；新增 WECOM_DISPATCHER_USER_ID=3。若核验不一致先停止配置并反馈。确认杨艳桢在现有企微应用可见范围内。

重建 ASS，启动自动给 notification_outbox 增加 event_type（旧行默认 ASSIGNMENT）和 target_user_id 两列；不更改原密钥、通知开关或其他账号。消息只含单号和处理入口，不含客户联系资料。专用派单员缺失/角色已撤销时 BLOCKED，缺企微绑定时 NOT_BOUND，不影响申请保存。

验证：隔离环境已覆盖去重、发送对象、撤权不发、已派单不发和原工程师通知回归。生产不代发测试消息，由用户提交一条新售后申请，核验杨艳桢收到提醒及对应 outbox 行为 SENT，再按原流程派单给工程师。回滚使用原镜像，保留兼容新增列，不恢复旧库覆盖新增工单。
