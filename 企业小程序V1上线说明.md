# 企业小程序 V1 上线说明

本版本包含一个微信小程序入口和两个业务模块：销售 CRM、ASS（After Sales Service，售后服务）。生产界面不再生成演示客户、演示工单、默认账号或固定待办数字。

## 数据口径

- 企业工作台只负责身份、角色和模块入口，不自行猜测 CRM 或 ASS 的业务数量。
- CRM 首页数据全部来自 CRM 服务端；空库显示 0 和空状态。
- ASS 首页和工单列表使用同一套查询与筛选条件；空库显示 0 和“暂无工单”。
- 测试数据只能存在于自动化测试夹具中，不能在生产服务启动时自动写入。

## 本次重新发布顺序

1. 先备份服务器上的 CRM JSON、ASS SQLite 和上传图片。
2. CRM 保留真实员工授权信息，但业务 JSON 换为空库；确认认证模式为 `WECHAT`。
3. ASS 使用新的空数据库文件启动，不复用旧 `aftersales.db`；已授权员工再次进入时由统一身份网关自动建立业务账号。
4. 重建并启动 CRM、ASS 两个容器，分别确认健康检查成功。
5. 验证王黎明进入 ASS 后，首页待处理为 0，工单列表为空；新建一张工单后，两处均为 1。
6. 在微信开发者工具上传新的企业服务小程序体验版，让测试人员从体验版入口进入。

注意：只重新上传微信小程序不会清除服务器旧数据库，第 1–4 步必须先完成。

## 推荐架构

当前不是“编程语言太多”，而是前端实现方式较分散：微信原生 JS/WXML/WXSS、CRM React、ASS 原生 JavaScript，以及 Python 服务端。短期继续使用 JavaScript + Python 风险可控，不建议为统一语言而重写稳定后端。

下一阶段建议统一为：

- 客户端：TypeScript；微信小程序壳保持轻量，CRM/ASS 共用一套组件与接口类型。
- Web 界面：React + TypeScript，逐步把 ASS 的原生 JavaScript 页面迁入同一前端工程。
- 服务端：Python FastAPI 模块化单体，统一身份、CRM、ASS、Odoo 适配器按模块组织，暂不拆微服务。
- 数据库：生产最终统一 PostgreSQL；当前小规模验证可继续使用 SQLite/JSON，但必须有每日备份和迁移脚本。
- 接口：统一 `/api/v1`、同一错误格式、同一权限模型和审计日志。

这套组合保留两种语言：TypeScript 负责界面与小程序，Python 负责业务接口和 Odoo 集成。它比强行全栈改成一种语言更稳，也更适合当前 4GB 服务器。
