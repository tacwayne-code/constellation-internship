# CRM 测试环境敏感配置

> 仅限公司内部接手人员使用。本文包含测试系统密码，不得发送到公开群、外部代码仓库或公共网盘。

## 当前测试服务器

- CRM 经理测试入口：`http://[INTERNAL_IP_REDACTED]:8123/`
- CRM 服务：`crm-v01.service`
- Odoo 测试入口：`http://[INTERNAL_IP_REDACTED]:8018/`
- 2026-07-21 健康接口实测：`erpMode=ODOO_TEST`、`routeMode=AMAP_LIVE`

这表示当前 CRM 已连接 Odoo 测试版，并已启用高德实时 Web 服务 API，不需要重新开发对接代码。

## Odoo 测试账套

- 数据库：`inspiri_erp_test`
- 用户：`***REDACTED***`
- 密码：`***REDACTED***`

当前对接边界是搜索测试账套商品并创建报价草稿。CRM 不自动确认销售订单，不直接修改库存、采购或生产数据。

## 高德 Web 服务 API

- 状态：已接通，服务器健康接口返回 `routeMode=AMAP_LIVE`。
- Key 保存位置：当前测试服务器的 CRM 私有环境配置中。
- 本地源码只有 `deploy/crm.env.example` 占位模板；不要把 Key 写入 `src/`、`dist/` 或 `wechat-miniapp/`。

接手现有测试服务器时不要用 `deploy/crm.env.example` 覆盖服务器已有环境文件。正式迁移到新云服务器时，应由公司在高德开放平台申请“Web服务”Key，并写入新服务器私有环境文件；不要长期依赖当前测试服务器上的 Key。

## 接手注意事项

1. 发布前备份 `/home/sameng/apps/crm-v01/shared/`。
2. 不要删除或覆盖服务器现有 CRM 环境文件、`shared-db.json` 和 `employees.json`。
3. 上传新代码后通过 `http://127.0.0.1:8123/api/health` 检查 `erpMode` 仍为 `ODOO_TEST`、`routeMode` 仍为 `AMAP_LIVE`。
4. 新云服务器需申请公司名下的高德 Web 服务 Key，配置为 `AMAP_WEB_SERVICE_KEY` 后重启 CRM 服务。
5. 如测试账号或密码以后变更，只修改服务器私有环境文件，不修改前端代码。
6. 若此压缩包需要发给公司外部人员，应先移除本文档并更换测试密码。
