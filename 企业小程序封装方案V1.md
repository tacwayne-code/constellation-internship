# CRM 与售后工单企业小程序封装方案 V1

## 结论

采用 **一个微信小程序 AppID + 一个企业身份中心 + CRM、售后工单两个业务模块** 的封装方式。员工进入小程序后由服务端返回其可使用的模块和权限；首页只显示有权限的入口。

这不是把两个系统重做一遍：现有 CRM 的 H5 页面、售后工单的原生页面与后端服务继续复用。V1 先统一入口、身份、权限、域名和发布流程，再分阶段把高频页面改成原生小程序页面。

## 本次确认：派单身份已包含

售后工单中的正式角色名为 **派单员**（系统内部代码：`paidan`）。它不是工程师的附属权限，而是独立的工作台：

| 角色 | 可见模块 | 主要能力 | 不应拥有的能力 |
| --- | --- | --- | --- |
| 销售人员 | CRM | 客户、商机、拜访、本人数据 | 全员销售数据、派单 |
| 销售经理 | CRM | 团队客户、商机、拜访、团队数据 | 售后派单（除非另授予） |
| 派单员 | 售后工单 | 新建工单、选择/改派工程师、查看全部工单、工程师管理、催办、导出 | 代替工程师提交维修记录 |
| 售后工程师 | 售后工单 | 本人待办、接单、到场/维修记录、完工上传 | 查看或改派其他工程师工单 |
| 系统管理员 | 两个模块 | 人员授权、角色配置、审计查看 | 不直接替代业务操作（除非被明确授予业务角色） |

已有售后源码已经具备派单员与工程师页面，以及派单员创建工单、指定工程师和工程师管理的后端校验。V1 的工作是把该身份接入统一微信身份，而不是新增一个临时的“派单入口”。

## 用户看到的结构

```text
企业服务工作台（一个 AppID）
├─ 首页
│  ├─ 销售 CRM              ← 有 CRM 权限才显示
│  └─ 售后服务 / 工单        ← 有售后权限才显示
├─ CRM
│  ├─ 客户、商机、拜访、销售任务
│  └─ 团队视图（仅销售经理）
└─ 售后服务
   ├─ 派单员：待派池、建单、派单/改派、工程师、工单统计
   └─ 工程师：我的待办、现场维修、维修记录、历史工单
```

同一个员工可以有多个角色。例如售后主管同时负责销售时，首页同时展示 CRM 和售后服务；进入不同模块后采用对应的数据范围。前端不提供可自行抬高权限的角色切换按钮。

## 技术封装边界

```text
微信小程序
  └─ wx.login() 取得一次性 code
       └─ 企业业务网关 /auth/wechat/login
            ├─ 用 AppID 对应的微信身份换取员工映射
            ├─ 查询角色与数据范围
            ├─ 返回 modules、permissions、短期业务令牌
            └─ CRM 服务 / 售后工单服务 / Odoo 适配层
                                      └─ Odoo
```

- 小程序只调用公司的 HTTPS 业务网关；不直接连接 Odoo，也不保存 Odoo 账号密码。
- 微信上下文只负责登录身份、入口，以及在需要时提供拍照、位置、订阅消息等能力；客户、工单和 CRM 的业务规则由企业服务端负责。
- CRM 当前可继续通过 `web-view` 打开已部署的 H5；售后高频操作（接单、拍照、定位、维修记录）优先使用原生页面。
- 任何 `web-view` 页面都必须使用已备案 HTTPS 域名，并在微信公众平台配置为业务域名；接口域名另配置为 request 合法域名。

## V1 目录建议

新建统一工程后，使用分包而非两个独立小程序，减少首次包体并维持一个 AppID：

```text
enterprise-miniapp/
├─ app.js / app.json / app.wxss
├─ pages/
│  ├─ launch/                # wx.login 与授权状态
│  └─ home/                  # 模块入口，按权限渲染
├─ packages/
│  ├─ crm/                   # CRM 原生壳与 web-view 入口
│  └─ service/
│      ├─ dispatch/          # 派单员工作台
│      └─ engineer/          # 工程师工作台
├─ services/
│  ├─ auth.js                # 登录、令牌续期、权限缓存
│  └─ request.js             # 统一请求、401 处理、审计头
└─ config.example.js         # 只含 HTTPS 域名，不含密钥
```

现有 `crm小程序/wechat-miniapp/` 和 `售后小程序/miniprogram/` 保留为可独立开发、回归和迁移的源码；在统一工程稳定前不删除它们。

## 身份与权限接口约定

小程序启动后只认服务端返回的身份，建议返回如下最小信息：

```json
{
  "employee": { "id": "EMP-018", "name": "王小明" },
  "modules": ["crm", "after_sales"],
  "roles": ["sales_manager", "paidan"],
  "permissions": ["crm.team.read", "workorder.create", "workorder.assign", "engineer.manage"],
  "accessToken": "短期令牌"
}
```

后端每个写操作仍必须再次校验权限。例如 `workorder.assign`、`workorder.reassign`、`engineer.manage` 只能由派单员或被明确授权的管理员执行；不能只依赖页面是否隐藏按钮。

## 发布前必须完成的事项

1. 注册并认证一个企业主体小程序，取得正式 AppID。
2. 将业务网关、CRM H5 和售后服务部署到已备案的 HTTPS 域名。
3. 在微信公众平台配置 request 合法域名、uploadFile 合法域名、downloadFile 合法域名；CRM 保留 web-view 时额外配置业务域名。
4. 在企业员工表中维护微信身份、员工、角色和数据范围的映射，并由管理员审核新员工申请。
5. 使用开发版验证：销售人员、销售经理、派单员、工程师、同时拥有两个模块的员工、无权限员工六类账号。
6. 确认派单、改派、接单、完工、取消工单均保留操作人和时间，便于后续审计。

## V1 的交付范围与下一步

V1 固化了单 AppID 的模块边界、派单角色、身份模型与迁移目录。下一次实现应先创建 `enterprise-miniapp/` 的登录页和按权限显示的首页，再将现有售后原生页面迁入 `packages/service/`；CRM H5 可先通过 web-view 平稳接入，等接口稳定后逐页原生化。
