# 售后需求报备与派单员交接

## 功能部署（Hermes）

1. 核对提交，备份 CRM 数据/员工库、ASS（SQLite backup API）、identity.db、平台身份副本、各服务配置与镜像。禁止提交凭据。保留用户本地小程序 project.config.json、project.private.config.json 的变动。
2. 只更新 CRM、service、identity 三个镜像/服务。CRM 与 ASS 继续使用既有同一 SSO_SHARED_SECRET；不轮换密钥，不变更 WeCom 或 Odoo 开关。CRM 可选 ASS_REQUEST_PAGE_URL 默认为 https://service.inspiri.cn/web/request.html，生产必须 HTTPS。CRM dist 必须重新构建。
3. ASS 自动增加 service_request_receipts 表，不清库、不修改历史工单。新的报备创建 SR- 前缀待派单工单，engineer_id=NULL、无自动通知；来源在 receipt 中保留，派单员详情可见报备人。原工单号不变。报备不自动新建 Odoo 客户，首版手工填写客户/电话，不包含图片上传。
4. 验证 CRM/ASS/identity 健康及既有数量不变。不代报备、不派单、不发测试消息。权限及流程测试在隔离测试库运行。未登陆 /service-requests 应401。
5. 本轮入口在 H5，不改原生小程序：工程师“我的任务”新增“提交售后需求 / 查看我的报备”；CRM 首页同名按钮；均跳转 ASS 共用表单。两个域名已配置为 web-view 业务域名，保留配置，线上需真机确认跨域跳转。通常无需重新上传小程序，退出业务模块再进入刷新 H5。

## 功能边界

- 报备票据只允许提交及读取本人最近50条报备，10分钟有效，不能登录 ASS 派单账号，不把销售伪装为派单员。过期/刷新后从原入口重新进入。
- 提交返回 SR 单号及“等待派单”。网络错误重试同一提交编号和内容，不重复建单；不同内容复用已成功编号返回409。丢响应后不要换编号重新报备，先重试/查看本人列表。
- 派单员在现有工单列表选择“待派单”，打开 SR 单→编辑工单，选故障类型和有效工程师后保存，状态自动为 assigned；此时产生企业微信通知。
- 工程师/销售无权自己指定工程师、查看他人报备或访问全局工单。本人可从报备入口刷新状态。报备来源随工单保留，本版不支持直接删除报备工单（409），避免破坏来源链。
- 当前未给派单员发送“新需求”企微通知，派单员从待派单队列处理。不包含附件、客户库联想与跨角色身份报备记录合并。

## 身份交接（需核实后执行，不凭微信号猜 openid）

用户确认：岗位名称“销售经理”就是基层销售人员，不是管理者。因此王黎明的 **CRM系统角色使用“销售人员”**，无团队管理/审批权限；不能赋予目前代码里拥有管理能力的“销售经理”角色。现有其他人的授权不擅自更改。

新派单员：杨艳桢，个人微信号 yyz1025400537。该微信号仅供微信公众平台添加体验成员，**不是 openid、企业微信 userid 或 ASS username**。

1. 保持现有授权可用，先在公众平台添加体验成员（用户操作）；新版本 identity 就绪后请杨艳桢扫码登录，出现“等待管理员授权。登记码：…”时，由本人把登记码交给用户确认。
2. Hermes 按 identity.db 中真实 subject 的 SHA256 前16位匹配该登记码，并由用户确认确实是杨艳桢本人提供；唯一匹配才授权，不能仅按姓名、唯一新记录或登录时间猜。登记码是定位线索，不是登录密码。没有本人确认则只读核验并停止身份写入。
3. 核实当前王黎明绑定，备份后为杨艳桢设置 ACTIVE、service_role=paidan、crm_role为空。同步平台身份副本并记录操作者、时间、旧新角色。通过本人真机登录让 ASS 正常建立账号，确认能看到原有工单（不要迁移旧 created_by）。
4. 确认新派单员可用后，再把王黎明网关 crm_role 改为“销售人员”、service_role清空，同步平台副本。**不能只改网关**：ASS 旧JWT仍有效，应将原 users 行 role 改为 disabled（保留id、姓名、历史工单created_by不动），使服务器端立即拒绝旧派单权限。旧短票据也会因为本地角色不一致而失败。切换后重新进入小程序/CRM建立销售会话。
5. 只撤销王黎明的 ASS 派单权限，不删除账号、工单、维修记录；不整体轮换JWT密钥导致所有工程师掉线。验证旧合法会话对创建/编辑/删除工单/全局统计等返回403；不借用他人会话或伪造票据。
6. 保留翁贻轩绑定和企微通知不动。杨艳桢企业微信 userid 尚未提供，本轮不猜、不拿个人微信号代填。派单通知收件人仍是工程师，不阻塞杨艳桢派单。王黎明原企微绑定可保留但不当作新派单员收件人。
7. 交接不能原子跨库完成，逐项验证并记录；任何异常停止后续步骤，按备份仅恢复本轮身份字段，不覆盖新增业务数据。

## 验收

王黎明 CRM 报备一单→杨艳桢待派单列表可见→选翁贻轩并保存→翁贻轩收到通知及任务；工程师也可独立报备一单，提交后不会出现在其执行任务中，直至被派单。测试单由用户创建保留，不代删。

## 本地验证

ASS 新需求3项测试、既有14项通知/流程测试，CRM shared_server 13项测试及构建；共用表单在390×844与1280×900本地浏览器模拟接口验证提交、禁止连点、成功状态与本人列表。未在生产创建任何测试客户/工单或发送消息。企业微信真机及身份实际交接由以上步骤完成。
